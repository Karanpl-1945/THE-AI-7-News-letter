# ── Groq ───────────────────────────────────────────────────
GROQ_API_KEY=your_groq_api_key_here
GROQ_SUMMARY_MODEL=llama-3.1-8b-instant
GROQ_EDITOR_MODEL=llama-3.3-70b-versatile
GROQ_MAX_RETRIES=2
GROQ_MAX_RETRY_WAIT_SECONDS=60
GROQ_REQUEST_TIMEOUT_SECONDS=45
GROQ_MIN_REQUEST_INTERVAL_SECONDS=6

# PostgreSQL
# Use the connection string supplied by your PostgreSQL provider.
# Keep SSL options in the URL when the provider includes them.
DATABASE_URL=postgresql://username:password@localhost:5432/ai_newsletter
DATABASE_CONNECT_TIMEOUT_SECONDS=10
# local | github_manual | github_schedule
WORKFLOW_TRIGGER=local

# Cloudflare R2 private artifact storage
R2_ACCOUNT_ID=your_cloudflare_account_id
R2_ACCESS_KEY_ID=your_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key
R2_BUCKET_NAME=ai-newsletter-artifacts
R2_PRESIGNED_URL_EXPIRY_SECONDS=3600

# ── Langfuse observability ─────────────────────────────────
LANGFUSE_PUBLIC_KEY=pk-lf-your_public_key
LANGFUSE_SECRET_KEY=sk-lf-your_secret_key
LANGFUSE_BASE_URL=https://cloud.langfuse.com

# ── GitHub ─────────────────────────────────────────────────
# Create at: https://github.com/settings/tokens  (no special scopes needed)
GITHUB_TOKEN=your_github_personal_access_token_here

# ── Email (Gmail) ──────────────────────────────────────────
# Enable 2FA on Gmail → https://myaccount.google.com/apppasswords
EMAIL_SENDER=your_gmail@gmail.com
EMAIL_PASSWORD=your_gmail_app_password_here
# gmail_smtp is the only transport today; swap this to add SES/Resend later.
EMAIL_TRANSPORT=gmail_smtp
# Receives the draft preview for review/approval — not the final subscriber send.
# Manage the subscriber list with: python -m database.subscriber_repository add <email>
ADMIN_EMAIL=kplohiya2005@gmail.com

# ── Review/subscriber API (one-tap email links) ────────────
# Public URL of the deployed FastAPI service (e.g. Render). Leave blank to
# disable clickable buttons and fall back to the review-newsletter workflow.
API_BASE_URL=
# Signs approve/request-changes/unsubscribe links so they can't be forged.
# Generate one with: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_SIGNING_SECRET=
# How long a review link stays valid, in seconds. Unsubscribe links never expire.
API_TOKEN_MAX_AGE_SECONDS=259200

# ── Personalisation ────────────────────────────────────────
USER_NAME=Your Name
# Comma-separated: LLMs, computer vision, reinforcement learning, NLP, etc.
USER_INTERESTS=LLMs,agents,computer vision
# beginner | intermediate | advanced
SKILL_LEVEL=intermediate

# ── Schedule ───────────────────────────────────────────────
# Day the newspaper is delivered (monday-sunday)
SCHEDULE_DAY=sunday
# Hour (24h clock, your local time)
SCHEDULE_HOUR=8
SCHEDULE_MINUTE=0
